var dispatcher = require('../utils/AppDispacter');
var api = require('../utils/api');


var serverActions = {
  searchCompleted: dispatcher.build('LOAD_ARTIST_COMPLETED')
};

module.exports = {
  loadArtistDetails: function(id){

  }
}
